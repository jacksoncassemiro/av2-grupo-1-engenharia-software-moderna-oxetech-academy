# Apresentação final

`apresentacao-final-av2-equipe01.pptx` — 17 slides, seguindo o roteiro definido em
`docs/13-papeis-e-responsabilidades.md` (capa · problema/proposta · escopo dentro/fora ·
backlog · arquitetura backend MVC · arquitetura frontend · modelo de dados · 3 práticas de
Clean Code · 2 Design Patterns · conflitos/ADRs · Git Flow/board · plano de testes · pipeline
de CI · demonstração ao vivo · métricas · melhorias futuras · retrospectiva).

16:9, tema próprio usando a cor da aplicação (`clinica` shade 8, `#28726F`, de
`frontend/src/theme.ts`).

O slide 14 ("Demonstração") é só o roteiro — a demonstração em si é ao vivo, contra o stack
Docker local (`localhost:3000`).

Gerado por script Python (`python-pptx`) a partir do conteúdo consolidado em `docs/` e do
resultado real de QA desta sessão, em 01/08/2026.
